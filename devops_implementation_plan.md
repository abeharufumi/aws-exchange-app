# DevOps・CI/CD実装設計書

本ドキュメントは、アプリの品質保証とリリース自動化を目的とした、DevOps基盤（自動テスト・CI/CD）の導入ステップを定義します。

## 目的
- 人間による手動テスト項目を減らし、品質をシステム的に担保する
- バグが混入したコードを本番環境（AWS）にデプロイさせない仕組みを作る
- 単体テストから本番デプロイまでをシームレスに自動化し、市場価値の高い開発プロセスを実現する

---

## 🚀 実装ステップ

### フェーズ 1: バックエンドの自動テスト基盤構築（Pytest）
まずは、自動で実行するための「テストプログラム自体」を作成します。
1. `pytest` および非同期テスト用モジュール（`httpx`, `pytest-asyncio`等）のインストール
2. テスト用データベースの分離（本番・開発に影響を与えないようにする）
3. コア機能のテスト実装:
   - 認証（サインアップ、ログインによるJWTトークン取得）
   - 基本API（ユーザー情報取得など）のステータスコード・レスポンス検証

### フェーズ 2: ローカル環境のコミット前チェック（pre-commit）
「手元でミスに気付ける仕組み」を構築し、開発体験(DX)を向上します。
1. `pre-commit` パッケージの導入
2. `.pre-commit-config.yaml` の作成
3. 以下のチェックを自動実行するように構成:
   - **Frontend:** TypeScriptの型チェック (`npx tsc --noEmit`)
   - **Backend:** Pythonの静的構文解析チェック（不要なimportの検出等）
4. コミット実行（VS CodeのUIからのコミット含む）時に、エラーがあれば自動でキャンセルされることを確認。

### フェーズ 3: リモートでの自動テストパイプライン構築（GitHub Actions - CI）
「リモート環境での第三者的な検証」を自動化します。
1. `.github/workflows/test.yml` の作成
2. `main` ブランチへのプッシュ、またはプルリクエスト作成時に以下を自動実行:
   - バックエンドの `pytest` 全件実行
   - フロントエンドの `npm run build` または型チェック
3. テストが失敗した場合はGitHub上で「×（Failed）」となり、マージをブロックするルール設定の確認。

### フェーズ 4: 本番デプロイの完全自動化（GitHub Actions - CD）
現在の `deploy_ecs.py` 等の手動運用を廃止し、セキュアな自動リリースを構築します。
1. `.github/workflows/deploy.yml` の作成
2. **発動条件**: `main` ブランチへのマージ完了 ＋ フェーズ3の「全テスト成功」
3. 自動化するプロセス:
   - AWSアカウントへの安全な認証（OIDCの利用）
   - Dockerイメージのビルドと Amazon ECR への自動Push
   - Amazon ECS (Fargate) サービスの最新化（Blue/Greenデプロイ等）
   - CloudFront のキャッシュ無効化

---

## 取り決め事項
- 全てのスクリプトやパイプライン実装は `.copilot-instructions.md` のルール（インフラを汚さない、安全なAPIキー管理など）に準拠します。
- AWSのクレデンシャル等はGitHubのスクリプト内に直書きせず、必ず GitHub Secrets を使用して安全に運用します。
